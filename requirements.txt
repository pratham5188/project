numpy>=1.24.0,<2.0
pandas>=2.0.0
plotly>=5.15.0
streamlit>=1.47.0
scikit-learn>=1.3.0
yfinance>=0.2.18
xgboost>=2.0.0
textblob>=0.17.1

# Optional dependencies for enhanced functionality
# Install with: pip install -r requirements.txt
# For full functionality, also install:
# pip install lightgbm>=4.0.0
# pip install tensorflow>=2.13.0
# pip install keras>=3.0.0
# pip install prophet>=1.1.4
# pip install jax[cpu]>=0.4.0

# These packages have fallback implementations if not available
# lightgbm>=4.0.0  # Used in Stacking Ensemble (fallback: XGBoost alternative)
# tensorflow>=2.13.0  # Used in GRU/LSTM models (fallback: simple prediction)
# keras>=3.0.0  # Used in some models (fallback: sklearn-based methods)
# prophet>=1.1.4  # Used in Prophet model (fallback: trend-based prediction)
# jax[cpu]>=0.4.0  # Used in some models (fallback: standard implementation)